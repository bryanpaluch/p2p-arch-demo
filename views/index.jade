extends layout

block content
  .container
    .row
      #dashboard.col-sm-12
        each server, i in servers
          if ((i % 6) == 0)
            .row
          .col-sm-4
            include shared/server_panel.jade

block extrajs
  script(src="/js/libs/require.js", data-main='/js/app.js') 
